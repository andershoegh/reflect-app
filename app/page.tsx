import SortableList from '@/lib/DnD/SortableList';
import { Test } from '@/lib/DnD/Test';

export default function Home() {
    return (
        <main className="flex min-h-screen flex-col items-center">Home</main>
    );
}
